name: Legacy Flask CI/CD (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current deployment path
        run: |
          echo "The legacy Flask CI/CD workflow is disabled."
          echo "The active branch uses C++ Drogon backend, Vue frontend, MySQL and Redis."
          echo "Use cpp-vue-ci.yml plus the documented deployment scripts for release validation."
