name: Legacy Flask CI (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current CI path
        run: |
          echo "The Flask/Python CI path is legacy and no longer runs automatically."
          echo "Use .github/workflows/cpp-vue-ci.yml for the active C++ Drogon + Vue stack."
