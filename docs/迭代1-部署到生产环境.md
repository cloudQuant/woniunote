现在本地测试没有问题了，现在准备部署到生产环境上，还存在很多的问题需要解决：
1. ✅ 生产环境是ubuntu22.04,需要安装vcpkg和相应的包 → **已完成**: `scripts/setup_ubuntu.sh`
2. ✅ 生产环境使用了https, 现在项目有这个功能吗？ → **支持**: 通过 Nginx 反向代理实现 HTTPS，配置文件 `configs/woniunote_nginx_prod.conf`
3. ✅ 脚本已创建:
   - **环境配置脚本**: `scripts/setup_ubuntu.sh` - 安装所有依赖
   - **生产启动脚本**: `scripts/start_prod.sh` - 启动服务
   - **生产停止脚本**: `scripts/stop_prod.sh` - 停止服务
   - **生产重启脚本**: `scripts/restart_prod.sh` - 重启服务
   - **生产配置模板**: `backend_cpp/config.prod.json` - 生产环境配置
   - **Nginx配置**: `configs/woniunote_nginx_prod.conf` - HTTPS + 反向代理

---

## 新创建的文件

| 文件 | 说明 |
|------|------|
| `scripts/setup_ubuntu.sh` | Ubuntu 22.04 环境配置脚本，安装 vcpkg、MySQL、Redis、Nginx、Node.js 等 |
| `scripts/start_prod.sh` | 生产环境启动脚本 |
| `scripts/stop_prod.sh` | 生产环境停止脚本 |
| `scripts/restart_prod.sh` | 生产环境重启脚本 |
| `backend_cpp/config.prod.json` | 生产环境配置文件模板 |
| `configs/woniunote_nginx_prod.conf` | Nginx HTTPS 配置文件 |

---

## 原项目说明 (参考)
A blog and website (https://www.yunjinqi.top) written by python, flask, HTML, CSS, JavaScript and mysql.

User info

介绍
在ubuntu20.04上，安装好anaconda后，使用flask、html、css和mysql建的个人博客，目前已经在个人网站(yunjinqi.top)上运行。

安装教程
# 国内克隆项目
git clone https://gitee.com/yunjinqi/woniunote.git
# 国外克隆项目
git clone https://github.com/cloudQuant/woniunote.git
# 安装依赖包
pip install -r ./woniunote/requirements.txt
cd woniunote/woniunote
# 创建账户名和密码的配置文件
touch user_password_config.yaml
# 打开文件，写入账户名和密码，参考user_password_config_example.yaml
nano user_password_config.yaml
# 如果需要本地测试，在configs目录下，运行
openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365
# 返回到主目录
cd ../../..
# 安装
pip install -U ./woniunote
# 首次运行的时候，创建数据库：python common/create_database.py
cd woniunote/woniunote
# 启动博客: 在woniunote包目录下执行
python app.py
# 生产环境执行: 
nohup gunicorn -w 3 -b 0.0.0.0:8888 app:app > woniunote_run.log 2>&1 &
# 本地测试使用
nohup python app.py > woniunote_run.log 2>&1 &
测试项目
# 首先安装测试依赖：
pip install -r requirements.txt
# 安装 Playwright 浏览器：
playwright install
# 运行功能测试：
pytest . -v --cov=woniunote --cov-report=html -n auto
# 运行性能测试：
locust -f tests/test_performance.py --host=http://localhost:5000
个人网站
这套开源代码实现的个人网站: https://www.yunjinqi.top

待实现功能
4. 完成之后，更新readme.md